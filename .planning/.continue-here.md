---
context: default
phase: free-form-quality-refactoring
task: 5
total_tasks: 12
status: paused
last_updated: 2026-04-26T11:09:02Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Sessão de refactoring de qualidade fora do fluxo GSD. Foram feitas 4 das 12 melhorias
identificadas na análise técnica do codebase (ver abaixo).

Arquivos modificados mas NÃO commitados:
- `lib/util.lua` — adicionado M.normalizeName(s)
- `lib/config.lua` — adicionado Config:validate()
- `lib/bootstrap.lua` — chama cfg:validate() após load, loga warnings
- `modules/engine.lua` — REFATORAÇÃO PRINCIPAL: tick() decomposto em 11 métodos
- `tests/run.lua` — +5 testes novos (59 → 64 total)

**ANTES de continuar: rodar `startup test` in-game para validar todos os 64 testes.**
</current_state>

<completed_work>

- **Item 1 — Decompor engine.lua** ✅
  - `tick()` de 525 linhas → 70 linhas de orquestração
  - Extraídos: `_processRequest`, `_handleNoCandidate`, `_handleMeOffline`, `_handleCraft`,
    `_handleExport`, `_logSubstitution`, `_refreshRequests`, `_fetchColonyStats`,
    `_fetchBuildings`, `_fetchCitizens`, `_markAllWaitingRetry`
  - `goto` em `pickCandidate` eliminados via flag `tierBlocked`
  - Dois loops em `resolveBuildingForTarget` → 1 loop (exata > parcial)

- **Item 2 — Validação de config.ini no load** ✅
  - `lib/config.lua`: `Config:validate()` delega a `config_schema.validateUpdates()`
  - `lib/bootstrap.lua`: chama `cfg:validate()` e loga cada erro como warn

- **Item 3 — Centralizar normalização** ✅
  - `lib/util.lua`: `M.normalizeName(s)` — lower + trim + colapso de espaços
  - `engine.lua`: `strLower()` e `normalizeName()` locais removidos

- **Item 4 — Expandir testes** ✅
  - `util_normalizeName_transforma_corretamente`
  - `config_validate_aceita_config_valida`
  - `config_validate_rejeita_invalidos`
  - `engine_todos_itens_blocked_by_tier`
  - `engine_request_na_janela_retry_e_ignorado`
</completed_work>

<remaining_work>

**Item 5** — Budget checking centralizado
- Extrair lógica duplicada de `budget:tryConsume()` de `me.lua:65`, `minecolonies.lua:33`,
  `inventory.lua:20` para `Budget:withConsume(state, group, fn, ...)` em `modules/budget.lua`

**Item 6** — Padronizar tratamento de erro
- 4 padrões diferentes no projeto; padronizar em `(result, err)` onde `err = {code, msg, details}|nil`
- Afeta `me.lua`, `engine.lua`, tests

**Item 7** — Watchdog periódico de periféricos
- `Peripherals.discover()` chamado só no boot
- Adicionar re-check em `loopEngine` (já há infra em `engine:updateHealthSnapshot`)

**Item 8** — Remover `goto` restantes
- `minecolonies.lua:170,293` — goto continue_res, goto continue
- `logger.lua:107` — goto continue

**Item 9** — Schema JSON para `mappings.json`
- Adicionar `lib/schema.lua` com validação ao carregar mappings

**Item 10** — Persistência com versionamento/migration
- `data/state.json` sem migration path; adicionar em `modules/persistence.lua`

**Item 11** — Loops `while true` com proteção
- `config_cli.lua` (7x) e `mapping_cli.lua` (2x): verificar `nil` de `io.read()`

**Item 12** — Timeout em `safeCall()`
- `lib/util.lua:safeCall` sem timeout; periférico bloqueado trava indefinidamente
</remaining_work>

<decisions_made>

- Mantido `engine.lua` como arquivo único (CC:Tweaked tem overhead de require() e as funções são fortemente acopladas)
- Usado `ctx` (context table) para passar estado entre `_processRequest` e seus handlers (available é mutável — passar por referência é correto)
- `lib/errors.lua` adiado para item 6 (mudança invasiva)
- `goto` em `pickCandidate` → eliminado com flag `tierBlocked` + `else` aninhado (mais legível que funções auxiliares para este caso)
</decisions_made>

<blockers>
- Nenhum blocker técnico.
- **Ação humana pendente**: rodar `startup test` in-game para validar 64 testes antes de continuar.
</blockers>

## Required Reading (in order)

1. `modules/engine.lua` — leitura obrigatória antes de continuar; toda a lógica foi reorganizada
2. `modules/budget.lua` — necessário antes do item 5 (Budget:withConsume)
3. `modules/me.lua`, `modules/minecolonies.lua`, `modules/inventory.lua` — necessários para o item 5
4. `.planning/HANDOFF.json` — estado estruturado completo desta sessão

## Infrastructure State

- CC:Tweaked in-game: testes precisam ser rodados com `startup test` no computador do jogo
- Sem servidores ou watchers ativos

<context>
Análise completa do codebase foi feita por subagente Explore no início da sessão.
15 problemas identificados em 3 tiers. Abordados os 4 de maior impacto.
O maior risco desta sessão era o refactor de engine.lua: preservar 100% da lógica
enquanto decompunha a função. Feito cirurgicamente — toda lógica foi mantida idêntica,
apenas reorganizada em métodos com responsabilidade única.

O padrão `ctx` (context table) foi escolhido porque `_processRequest` precisa de
~8 variáveis do tick() e passá-las individualmente seria ruído. O `ctx.available`
é especialmente importante: é mutado a cada request processado para rastrear alocação
de itens já presentes no destino entre múltiplos requests no mesmo tick.
</context>

<next_action>
1. Rodar `startup test` in-game → confirmar 64/64 OK
2. Se OK: prosseguir com Item 5 — ler modules/budget.lua, me.lua, minecolonies.lua, inventory.lua
   e adicionar Budget:withConsume() eliminando as 3 cópias de tryConsume()
</next_action>
