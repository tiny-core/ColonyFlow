# Verificação: Phase 08

## Checklist

- [ ] `mappings.json` v2 é carregado corretamente.
- [ ] `mappings.json` v1 continua funcionando (fallback ou migração automática).
- [ ] `startup map` consegue editar equivalências e tiers no novo formato.
- [ ] `startup test` cobre parsing/migração do mapping e passa.

