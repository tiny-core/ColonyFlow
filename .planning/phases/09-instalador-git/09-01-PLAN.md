# Plan 09-01: Implementar instalador/atualizador via Git raw

## Contexto

Ver [09-CONTEXT.md](./09-CONTEXT.md).

## Entregáveis

- `install.lua` capaz de instalar/atualizar o projeto via HTTP.
- Manifesto de arquivos e política de preservação (config/dados).
- Teste manual guiado + checklist.

## Plano

1. Definir URL base (raw) do repositório e manifesto de arquivos.
2. Implementar download seguro (timeouts, retries simples, mensagens).
3. Implementar modo update com preservação por padrão.
4. Documentar uso (comandos) e pré-requisitos (HTTP).

