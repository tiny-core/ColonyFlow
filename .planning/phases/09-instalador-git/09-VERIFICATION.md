# Verificação: Phase 09

## Checklist

- [ ] Um computador vazio consegue instalar o projeto com `install.lua`.
- [ ] Atualização não apaga `config.ini` e `data/mappings.json` por padrão.
- [ ] Falha de HTTP mostra mensagem acionável.

