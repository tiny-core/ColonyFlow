# Plan 06-01: Cache de ME Bridge

## Contexto

Ver [06-CONTEXT.md](./06-CONTEXT.md).

## Entregáveis

- Cache configurável para ME:getItem / ME:listItems / ME:isCraftable com TTL.
- Testes garantindo hit/miss de cache e desativação via TTL=0.
- Atualização de config.ini com novas chaves.

## Plano

1. Implementar cache em `modules/me.lua` usando `state.cache` quando disponível.
2. Ler TTLs de `state.cfg` quando disponível; defaults seguros.
3. Adicionar testes unitários para comportamento do cache.
4. Atualizar `config.ini` e checklist de verificação.

