---
phase: 15-operabilidade-resiliencia-doctor-persistencia-circuit-breaker
plan: 01
type: execute
wave: 1
depends_on: ["12-01"]
requirements_addressed: []
files_modified: [
  startup.lua,
  modules/engine.lua,
  modules/me.lua,
  lib/util.lua,
  lib/config_schema.lua,
  tests/run.lua,
  .planning/phases/15-operabilidade-resiliencia-doctor-persistencia-circuit-breaker/15-VERIFICATION.md
]
autonomous: true
must_haves:
  truths:
    - `startup doctor` roda sem travar, imprime diagnostico ASCII acionavel e retorna exit code apropriado quando suportado.
    - Jobs em andamento sobrevivem a reboot via `data/state.json` e evitam duplicar crafts apos reinicio.
    - Circuit breaker reduz spam de chamadas ao ME quando offline/oscilando e retoma automaticamente quando voltar.
  artifacts:
    - startup.lua
    - modules/engine.lua
    - .planning/phases/15-operabilidade-resiliencia-doctor-persistencia-circuit-breaker/15-VERIFICATION.md
---

# Phase 15 Plan 01: Operabilidade + Resiliencia (doctor + persistencia + circuit breaker)

## Objective

- **What:** Adicionar `startup doctor`, persistir jobs e introduzir circuit breaker/backoff do ME.
- **Why:** Melhorar diagnostico e confiabilidade em operacao longa (SP/MP) e reduzir duplicidade/instabilidade em reboot e ME oscilando.
- **Output:** Doctor in-game + persistencia leve + resiliencia do ME + testes + checklist.

## Tasks

<task type="code">
  <name>Adicionar modo `startup doctor`</name>
  <files>startup.lua</files>
  <read_first>
    - startup.lua
    - lib/bootstrap.lua
    - lib/config_schema.lua
    - modules/peripherals.lua
    - modules/me.lua
  </read_first>
  <action>
    - Adicionar `if mode == "doctor" then ... end` no `startup.lua`.
    - Implementar rotina doctor em um modulo dedicado (ex.: `modules/doctor.lua`) ou inline pequena, que:
      - carrega config.ini (com defaults)
      - valida configuracao via `lib/config_schema.lua` (ou verificacoes minimas)
      - checa perifericos essenciais (present/absent)
      - checa ME grid online (quando meBridge existir) via `ME:isOnline()`
      - checa HTTP API (http.get existe) e, se possivel, checkURL para manifest
      - imprime resumo ASCII + sugestoes de acao
  </action>
  <verify>
    - Manual: rodar `startup doctor` in-world
  </verify>
</task>

<task type="code">
  <name>Persistencia de jobs (data/state.json) e retomada no boot</name>
  <files>modules/engine.lua, lib/util.lua</files>
  <read_first>
    - modules/engine.lua
    - lib/util.lua
  </read_first>
  <action>
    - Definir schema minimo de persistencia (jobs/work + timestamps).
    - Salvar periodicamente (ex.: a cada N segundos ou quando work muda) usando `Util.writeFileAtomic`.
    - No bootstrap/engine init, carregar `data/state.json` e reconciliar com o estado atual:
      - expirar jobs antigos
      - evitar duplicar craft (se ja estiver marcado craftando/await, manter)
  </action>
  <verify>
    - Manual: iniciar um job, reiniciar e confirmar que retoma sem duplicar craft
  </verify>
</task>

<task type="code">
  <name>Circuit breaker/backoff do ME</name>
  <files>modules/me.lua, modules/engine.lua</files>
  <read_first>
    - modules/me.lua
    - modules/engine.lua
  </read_first>
  <action>
    - Trackear falhas consecutivas de `ME:isOnline()` e entrar em modo degraded por um periodo.
    - Enquanto degraded, evitar chamadas pesadas ao ME (getItem/list/isCraftable) e retornar erro padronizado `me_offline`.
    - Registrar em `state` um snapshot de degraded e proximo retry para UI/log.
  </action>
  <verify>
    - Manual: desligar ME e confirmar que reduz spam e se recupera quando voltar
  </verify>
</task>

<task type="code">
  <name>Testes unitarios basicos (persistencia + circuit breaker)</name>
  <files>tests/run.lua</files>
  <read_first>
    - tests/run.lua
    - lib/util.lua
    - modules/me.lua
  </read_first>
  <action>
    - Mockar fs para testar escrita/parse de `data/state.json` sem tocar no disco real.
    - Mockar meBridge para simular ME offline/online e validar logica de backoff/circuit breaker.
  </action>
  <verify>
    - `startup test`
  </verify>
</task>
