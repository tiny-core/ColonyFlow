---
phase: 20-roteamento-multi-destino
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/config_cli.lua
  - tests/run.lua
autonomous: true
requirements:
  - phase-20
must_haves:
  truths:
    - "O menu principal do Config CLI tem uma entrada 'Roteamento de destino'"
    - "Ao entrar no menu de roteamento, as 11 classes sao listadas com valor atual ao lado"
    - "Usuario pode editar o nome do periferico de cada classe ou deixar vazio para limpar"
    - "Mudancas sao salvas via saveIni (mesma logica atomica dos outros menus)"
    - "Os 4 testes de roteamento passam no harness de testes do CC"
  artifacts:
    - path: "modules/config_cli.lua"
      provides: "runDeliveryRoutingMenu + integracao no main()"
      contains: "runDeliveryRoutingMenu"
    - path: "tests/run.lua"
      provides: "4 testes de roteamento D-08"
      contains: "routing_classe_mapeada_e_online"
  key_links:
    - from: "modules/config_cli.lua:main"
      to: "runDeliveryRoutingMenu"
      via: "elseif choice == 'Roteamento de destino'"
      pattern: "Roteamento de destino"
    - from: "tests/run.lua"
      to: "resolveRoutedTarget logic (inline stub)"
      via: "makeCfg + peripheral stub"
      pattern: "routing_classe_mapeada_e_online"
---

<objective>
Implementar o menu de Config CLI para a secao delivery_routing e adicionar os 4 testes de roteamento ao harness de testes.

Purpose: Sem o menu CLI o usuario nao pode configurar destinos por classe in-game. Sem os testes nao ha regressao automatica para a logica de fallback. Esta plano entrega D-06 e D-08.

Output: modules/config_cli.lua com runDeliveryRoutingMenu e entrada no menu principal; tests/run.lua com 4 testes de roteamento.
</objective>

<execution_context>
@P:\Minecraft\ColonyFlow\.planning\phases\20-roteamento-multi-destino\20-CONTEXT.md
@P:\Minecraft\ColonyFlow\.planning\phases\20-roteamento-multi-destino\20-PATTERNS.md
</execution_context>

<context>
@P:\Minecraft\ColonyFlow\.planning\ROADMAP.md

<interfaces>
<!-- Interfaces extraidas dos arquivos que serao modificados. -->

From modules/config_cli.lua — estruturas existentes que devem ser espelhadas:

```lua
-- FIELD_LABELS (linha 21): tabela de labels por secao.key
local FIELD_LABELS = {
  delivery = {
    default_target_container = "Destino padrao (inventario)",
    -- ...
  },
  -- adicionar delivery_routing aqui (mesma profundidade)
}

-- buildEffective(cfg, updates) (linha 335): retorna tabela com valores atuais ou pendentes
-- adicionar delivery_routing = { armor_helmet = v("delivery_routing","armor_helmet"), ... }

-- buildChangedOnly(cfg, updates) (linha 375): detecta mudancas
-- local out = { peripherals={}, core={}, delivery={}, update={} }
-- adicionar delivery_routing={} e nil-check correspondente

-- saveIni(cfg, updates) (linha 413): guarda check de nil
-- if changedOnly.peripherals == nil and ... and changedOnly.update == nil then
-- estender com: and changedOnly.delivery_routing == nil

-- runDeliveryMenu(cfg, updates) (linha 686): template completo para novo menu
-- funcoes auxiliares disponiveis: trim, prompt, selectList, separatorColor, loadCfg, saveIni

-- main() (linha 778): inicializa updates e menu principal
-- local updates = { peripherals={}, core={}, delivery={}, update={} }
-- labels = { "Perifericos","Core+Logs","Delivery","Update-check","Sair" }
```

From tests/run.lua — estruturas reutilizadas:

```lua
-- makeCfg(values): stub de Config com get/getList/getNumber/getBool
-- Uso: makeCfg({ delivery={default_target_container="rack_0"}, delivery_routing={armor_helmet="rack_tools_0"} })

-- assertEq(a, b, msg): assert com mensagem de erro clara

-- Stub de peripheral:
-- local oldPeripheral = peripheral
-- peripheral = { isPresent=function(name) return name=="rack_0" end, wrap=function(name) return {} end }
-- ... teste ...
-- peripheral = oldPeripheral

-- runTest(name, fn): registra OK/FAIL/SKIP

-- local tests = { { "nome_do_teste", function() ... end }, ... }
-- (a lista e iterada e runTest chamado para cada entrada)
```

Logica de resolveRoutedTarget (para replicar inline nos testes — D-08):

```lua
-- Esta e a logica que os testes devem validar (do Plan 01):
local function resolveRoutedTarget(cfg, itemName)
  local class = guessClass(itemName)  -- retorna string ou nil
  if class then
    local routedName = cfg:get("delivery_routing", class, "")
    if routedName ~= "" and peripheral.isPresent(routedName) then
      return routedName, peripheral.wrap(routedName)
    end
  end
  return resolveTarget(cfg)  -- fallback
end

-- guessClass e resolveTarget tambem precisam ser reimplementados inline nos testes
-- (local functions em engine.lua, nao exportadas via require)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar menu delivery_routing ao Config CLI</name>
  <files>modules/config_cli.lua</files>
  <read_first>
    - modules/config_cli.lua linhas 21-50 (FIELD_LABELS — ponto de insercao da nova secao)
    - modules/config_cli.lua linhas 335-441 (buildEffective, buildChangedOnly, saveIni — pontos de extensao)
    - modules/config_cli.lua linhas 686-730 (runDeliveryMenu — template para copiar)
    - modules/config_cli.lua linhas 778-820 (main — adicionar updates.delivery_routing e entrada no menu)
  </read_first>
  <action>
Fazer 5 edicoes cirurgicas em modules/config_cli.lua seguindo a ordem abaixo. Cada edicao e independente e nao depende das outras para ser localizada.

**Edicao 1 — FIELD_LABELS: adicionar secao delivery_routing (apos a entrada "update", antes do fechamento da tabela)**

```lua
  delivery_routing = {
    armor_helmet      = "Helmet",
    armor_chestplate  = "Chestplate",
    armor_leggings    = "Leggings",
    armor_boots       = "Boots",
    tool_pickaxe      = "Pickaxe",
    tool_shovel       = "Shovel",
    tool_axe          = "Axe",
    tool_hoe          = "Hoe",
    tool_sword        = "Sword",
    tool_bow          = "Bow",
    tool_shield       = "Shield",
  },
```

**Edicao 2 — buildEffective: adicionar delivery_routing no return (apos a entrada "update = {...}")**

```lua
    delivery_routing = {
      armor_helmet     = v("delivery_routing", "armor_helmet"),
      armor_chestplate = v("delivery_routing", "armor_chestplate"),
      armor_leggings   = v("delivery_routing", "armor_leggings"),
      armor_boots      = v("delivery_routing", "armor_boots"),
      tool_pickaxe     = v("delivery_routing", "tool_pickaxe"),
      tool_shovel      = v("delivery_routing", "tool_shovel"),
      tool_axe         = v("delivery_routing", "tool_axe"),
      tool_hoe         = v("delivery_routing", "tool_hoe"),
      tool_sword       = v("delivery_routing", "tool_sword"),
      tool_bow         = v("delivery_routing", "tool_bow"),
      tool_shield      = v("delivery_routing", "tool_shield"),
    },
```

**Edicao 3 — buildChangedOnly: adicionar delivery_routing na inicializacao de out e nil-checks**

Localizar:
```lua
  local out = { peripherals = {}, core = {}, delivery = {}, update = {} }
```
Substituir por:
```lua
  local out = { peripherals = {}, core = {}, delivery = {}, update = {}, delivery_routing = {} }
```

Localizar (apos o loop):
```lua
  if next(out.update) == nil then out.update = nil end
  return out
```
Substituir por:
```lua
  if next(out.update) == nil then out.update = nil end
  if next(out.delivery_routing) == nil then out.delivery_routing = nil end
  return out
```

**Edicao 4 — saveIni: estender o check de "nenhuma mudanca"**

Localizar:
```lua
  if changedOnly.peripherals == nil and changedOnly.core == nil and changedOnly.delivery == nil and changedOnly.update == nil then
```
Substituir por:
```lua
  if changedOnly.peripherals == nil and changedOnly.core == nil and changedOnly.delivery == nil and changedOnly.update == nil and changedOnly.delivery_routing == nil then
```

**Edicao 5 — Adicionar runDeliveryRoutingMenu e modificar main()**

Inserir a funcao `runDeliveryRoutingMenu` logo apos o fim de `runDeliveryMenu` (linha ~730, antes de `runUpdateMenu`):

```lua
local ROUTING_CLASS_KEYS = {
  "armor_helmet", "armor_chestplate", "armor_leggings", "armor_boots",
  "tool_pickaxe", "tool_shovel", "tool_axe", "tool_hoe", "tool_sword", "tool_bow", "tool_shield",
}

local function runDeliveryRoutingMenu(cfg, updates)
  while true do
    local eff = buildEffective(cfg, updates).delivery_routing
    local labels = {}
    for _, key in ipairs(ROUTING_CLASS_KEYS) do
      local label = fieldLabel("delivery_routing", key)
      local cur   = trim(eff[key] or "")
      local suffix = cur ~= "" and ("(" .. cur .. ")") or "()"
      table.insert(labels, { text = label, suffix = suffix, suffixColor = separatorColor() })
    end
    table.insert(labels, { separator = true })
    table.insert(labels, { text = "Salvar",  action = "save" })
    table.insert(labels, { text = "Voltar",  action = "back" })

    local idx, why = selectList("Roteamento de destino", "Enter confirma | <- volta", labels, 1)
    if why ~= "enter" or not idx then return end
    local chosen = labels[idx]
    if type(chosen) == "table" and chosen.action == "back" then
      return
    end
    if type(chosen) == "table" and chosen.action == "save" then
      cfg = loadCfg()
      saveIni(cfg, updates)
      cfg = loadCfg()
    else
      -- idx 1..11 correspondem a ROUTING_CLASS_KEYS
      local key = ROUTING_CLASS_KEYS[idx]
      if key then
        local eff2 = buildEffective(cfg, updates).delivery_routing
        local v = prompt(fieldLabel("delivery_routing", key), trim(eff2[key] or ""))
        -- Permite string vazia (limpa o mapeamento — D-06)
        if v ~= nil then
          updates.delivery_routing[key] = trim(v)
        end
      end
    end
  end
end
```

Em main(), localizar:
```lua
  local updates = {
    peripherals = {},
    core = {},
    delivery = {},
    update = {},
  }
```
Substituir por:
```lua
  local updates = {
    peripherals = {},
    core = {},
    delivery = {},
    update = {},
    delivery_routing = {},
  }
```

Localizar a lista de labels do menu principal:
```lua
    local labels = {
      "Perifericos",
      "Core+Logs",
      "Delivery",
      "Update-check",
      "Sair",
    }
```
Substituir por:
```lua
    local labels = {
      "Perifericos",
      "Core+Logs",
      "Delivery",
      "Roteamento de destino",
      "Update-check",
      "Sair",
    }
```

Localizar o bloco elseif que chama runDeliveryMenu e adicionar o elseif para o novo menu imediatamente apos:
```lua
    elseif choice == "Delivery" then
      cfg = loadCfg()
      runDeliveryMenu(cfg, updates)
    elseif choice == "Roteamento de destino" then
      cfg = loadCfg()
      runDeliveryRoutingMenu(cfg, updates)
    elseif choice == "Update-check" then
```
  </action>
  <verify>
    <automated>cd /d P:\Minecraft\ColonyFlow && lua -e "local s=io.open('modules/config_cli.lua','r'):read('*a'); assert(s:find('runDeliveryRoutingMenu'), 'funcao ausente'); assert(s:find('Roteamento de destino'), 'menu entry ausente'); assert(s:find('delivery_routing = {}'), 'updates.delivery_routing ausente'); assert(s:find('changedOnly%.delivery_routing'), 'nil check ausente'); print('OK')"</automated>
  </verify>
  <done>
    - FIELD_LABELS tem entrada delivery_routing com 11 classes
    - buildEffective retorna delivery_routing com os 11 valores
    - buildChangedOnly inicializa e nil-check delivery_routing
    - saveIni verifica changedOnly.delivery_routing == nil
    - runDeliveryRoutingMenu definida com loop while true, labels de 11 classes, Salvar/Voltar
    - main() inicializa updates.delivery_routing = {} e tem "Roteamento de destino" no menu principal
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar 4 testes de roteamento em tests/run.lua</name>
  <files>tests/run.lua</files>
  <read_first>
    - tests/run.lua linhas 1-100 (assertEq, makeCfg, inicio da tabela tests — para ver onde inserir e entender padroes de stub)
    - tests/run.lua linhas 500-570 (exemplos de testes com peripheral stub — padrão a copiar)
  </read_first>
  <behavior>
    - routing_classe_mapeada_e_online: cfg tem armor_helmet="rack_tools_0", peripheral "rack_tools_0" online -> resultado e "rack_tools_0"
    - routing_classe_mapeada_e_offline: cfg tem armor_helmet="rack_tools_0" OFFLINE, default "rack_0" online -> resultado e "rack_0"
    - routing_classe_nao_mapeada: cfg tem armor_helmet="" (vazio), default "rack_0" online -> resultado e "rack_0"
    - routing_item_sem_classe: item "minecraft:bread" (guessClass=nil), default "rack_0" online -> resultado e "rack_0"
  </behavior>
  <action>
Os testes nao podem fazer `require("modules.engine")` porque `resolveRoutedTarget` e `guessClass` sao `local function`. Usar o padrao de stub inline (igual a `makeCfg` que reimplementa a API de Config).

Localizar o final da tabela `local tests = { ... }` em tests/run.lua (antes do `}` de fechamento da tabela) e inserir as 4 entradas abaixo:

```lua
  { "routing_classe_mapeada_e_online", function()
    -- Arrange: helmet mapeado e online
    local cfg = makeCfg({
      delivery         = { default_target_container = "rack_0" },
      delivery_routing = { armor_helmet = "rack_tools_0" },
    })
    local oldPeripheral = peripheral
    peripheral = {
      isPresent = function(n) return n == "rack_tools_0" or n == "rack_0" end,
      wrap      = function(n) return { _name = n } end,
    }
    -- Logica inline de guessClass + resolveRoutedTarget
    local function guessClassStub(name)
      if not name then return nil end
      local lo = name:lower()
      if lo:find("helmet",1,true) then return "armor_helmet" end
      return nil
    end
    local function resolveTargetStub(c)
      local raw = c:get("delivery","default_target_container","")
      if raw ~= "" and peripheral.isPresent(raw) then return raw, peripheral.wrap(raw) end
      return nil, nil
    end
    local function resolveRoutedTargetStub(c, itemName)
      local class = guessClassStub(itemName)
      if class then
        local rn = c:get("delivery_routing", class, "")
        if rn ~= "" and peripheral.isPresent(rn) then return rn, peripheral.wrap(rn) end
      end
      return resolveTargetStub(c)
    end
    -- Act
    local name, inv = resolveRoutedTargetStub(cfg, "iron_helmet")
    peripheral = oldPeripheral
    -- Assert
    assertEq(name, "rack_tools_0", "routing_classe_mapeada_e_online: nome errado")
    assertEq(inv._name, "rack_tools_0", "routing_classe_mapeada_e_online: inv errado")
  end },

  { "routing_classe_mapeada_e_offline", function()
    -- Arrange: helmet mapeado mas offline; default online
    local cfg = makeCfg({
      delivery         = { default_target_container = "rack_0" },
      delivery_routing = { armor_helmet = "rack_tools_0" },
    })
    local oldPeripheral = peripheral
    peripheral = {
      isPresent = function(n) return n == "rack_0" end,  -- rack_tools_0 esta OFFLINE
      wrap      = function(n) return { _name = n } end,
    }
    local function guessClassStub(name)
      if not name then return nil end
      local lo = name:lower()
      if lo:find("helmet",1,true) then return "armor_helmet" end
      return nil
    end
    local function resolveTargetStub(c)
      local raw = c:get("delivery","default_target_container","")
      if raw ~= "" and peripheral.isPresent(raw) then return raw, peripheral.wrap(raw) end
      return nil, nil
    end
    local function resolveRoutedTargetStub(c, itemName)
      local class = guessClassStub(itemName)
      if class then
        local rn = c:get("delivery_routing", class, "")
        if rn ~= "" and peripheral.isPresent(rn) then return rn, peripheral.wrap(rn) end
      end
      return resolveTargetStub(c)
    end
    local name, inv = resolveRoutedTargetStub(cfg, "iron_helmet")
    peripheral = oldPeripheral
    assertEq(name, "rack_0", "routing_classe_mapeada_e_offline: deveria cair no default")
  end },

  { "routing_classe_nao_mapeada", function()
    -- Arrange: armor_helmet = "" (nao configurado); default online
    local cfg = makeCfg({
      delivery         = { default_target_container = "rack_0" },
      delivery_routing = { armor_helmet = "" },
    })
    local oldPeripheral = peripheral
    peripheral = {
      isPresent = function(n) return n == "rack_0" end,
      wrap      = function(n) return { _name = n } end,
    }
    local function guessClassStub(name)
      if not name then return nil end
      local lo = name:lower()
      if lo:find("helmet",1,true) then return "armor_helmet" end
      return nil
    end
    local function resolveTargetStub(c)
      local raw = c:get("delivery","default_target_container","")
      if raw ~= "" and peripheral.isPresent(raw) then return raw, peripheral.wrap(raw) end
      return nil, nil
    end
    local function resolveRoutedTargetStub(c, itemName)
      local class = guessClassStub(itemName)
      if class then
        local rn = c:get("delivery_routing", class, "")
        if rn ~= "" and peripheral.isPresent(rn) then return rn, peripheral.wrap(rn) end
      end
      return resolveTargetStub(c)
    end
    local name, inv = resolveRoutedTargetStub(cfg, "iron_helmet")
    peripheral = oldPeripheral
    assertEq(name, "rack_0", "routing_classe_nao_mapeada: deveria usar default")
  end },

  { "routing_item_sem_classe", function()
    -- Arrange: item sem classe reconhecida; default online
    local cfg = makeCfg({
      delivery = { default_target_container = "rack_0" },
    })
    local oldPeripheral = peripheral
    peripheral = {
      isPresent = function(n) return n == "rack_0" end,
      wrap      = function(n) return { _name = n } end,
    }
    local function guessClassStub(name)
      if not name then return nil end
      local lo = name:lower()
      if lo:find("helmet",1,true) then return "armor_helmet" end
      if lo:find("pickaxe",1,true) then return "tool_pickaxe" end
      -- "minecraft:bread" nao bate em nenhuma regra
      return nil
    end
    local function resolveTargetStub(c)
      local raw = c:get("delivery","default_target_container","")
      if raw ~= "" and peripheral.isPresent(raw) then return raw, peripheral.wrap(raw) end
      return nil, nil
    end
    local function resolveRoutedTargetStub(c, itemName)
      local class = guessClassStub(itemName)
      if class then
        local rn = c:get("delivery_routing", class, "")
        if rn ~= "" and peripheral.isPresent(rn) then return rn, peripheral.wrap(rn) end
      end
      return resolveTargetStub(c)
    end
    local name, inv = resolveRoutedTargetStub(cfg, "minecraft:bread")
    peripheral = oldPeripheral
    assertEq(name, "rack_0", "routing_item_sem_classe: deveria usar default")
    assertEq(inv, nil, "routing_item_sem_classe: inv deve ser nil quando resolve via wrap")
    -- Nota: resolveTargetStub com makeCfg retorna o nome mas wrap retorna tabela simples sem nil
    -- Correcao: assertEq nao e necessaria para inv aqui, apenas para name
  end },
```

ATENCAO: A ultima entrada `routing_item_sem_classe` tem um comentario sobre assertEq para `inv`. Simplificar: apenas verificar `name == "rack_0"`. A linha `assertEq(inv, nil, ...)` deve ser REMOVIDA — o stub de wrap retorna `{ _name = n }` (nao nil). So verificar o nome retornado.

Versao correta da ultima entrada (sem assertEq em inv):

```lua
  { "routing_item_sem_classe", function()
    local cfg = makeCfg({
      delivery = { default_target_container = "rack_0" },
    })
    local oldPeripheral = peripheral
    peripheral = {
      isPresent = function(n) return n == "rack_0" end,
      wrap      = function(n) return { _name = n } end,
    }
    local function guessClassStub(name)
      if not name then return nil end
      local lo = name:lower()
      if lo:find("helmet",1,true) then return "armor_helmet" end
      if lo:find("pickaxe",1,true) then return "tool_pickaxe" end
      return nil
    end
    local function resolveTargetStub(c)
      local raw = c:get("delivery","default_target_container","")
      if raw ~= "" and peripheral.isPresent(raw) then return raw, peripheral.wrap(raw) end
      return nil, nil
    end
    local function resolveRoutedTargetStub(c, itemName)
      local class = guessClassStub(itemName)
      if class then
        local rn = c:get("delivery_routing", class, "")
        if rn ~= "" and peripheral.isPresent(rn) then return rn, peripheral.wrap(rn) end
      end
      return resolveTargetStub(c)
    end
    local name = resolveRoutedTargetStub(cfg, "minecraft:bread")
    peripheral = oldPeripheral
    assertEq(name, "rack_0", "routing_item_sem_classe: deveria usar default")
  end },
```

Inserir as 4 entradas antes do `}` de fechamento da tabela `tests`. Manter a virgula apos cada entrada (exceto a ultima se for o ultimo elemento da tabela, ou adicionar virgula se houver mais entradas apos).
  </action>
  <verify>
    <automated>cd /d P:\Minecraft\ColonyFlow && lua -e "local s=io.open('tests/run.lua','r'):read('*a'); assert(s:find('routing_classe_mapeada_e_online'), 'teste 1 ausente'); assert(s:find('routing_classe_mapeada_e_offline'), 'teste 2 ausente'); assert(s:find('routing_classe_nao_mapeada'), 'teste 3 ausente'); assert(s:find('routing_item_sem_classe'), 'teste 4 ausente'); print('OK')"</automated>
  </verify>
  <done>
    - tests/run.lua contem as 4 entradas de teste de roteamento na tabela tests
    - Cada teste usa makeCfg + peripheral stub + stub inline de guessClass/resolveRoutedTarget
    - peripheral e restaurado (= oldPeripheral) apos cada teste
    - Todos os 4 testes passam quando executados no CC com `startup test`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal input -> config_cli | Usuario digita nome de periferico; valor e armazenado como string em config.ini |
| config.ini -> saveIni | Escrita atomica via Config.patchIniFileAtomic com backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | saveIni escrevendo delivery_routing em config.ini | mitigate | Config.patchIniFileAtomic ja e usado — escrita atomica com backup (keep_backups=2) |
| T-20-06 | Denial of Service | Usuario configura 11 perifericos invalidos; engine chama peripheral.isPresent 11x por request | accept | peripheral.isPresent e chamada CC rapida (nao IO bloqueante); max 11 checks por request e aceitavel |
| T-20-07 | Information Disclosure | Nomes de perifericos visiveis no menu CLI | accept | Ambiente local; informacao de configuracao operacional esperada |
</threat_model>

<verification>
Apos execucao deste plano:

1. lua -e "..." confirma runDeliveryRoutingMenu e "Roteamento de destino" em config_cli.lua
2. lua -e "..." confirma os 4 testes de roteamento em tests/run.lua
3. No CC: `startup config` -> menu principal exibe "Roteamento de destino" entre "Delivery" e "Update-check"
4. No CC: `startup test` -> os 4 testes routing_* aparecem como [OK]
</verification>

<success_criteria>
- modules/config_cli.lua: FIELD_LABELS tem delivery_routing com 11 classes; buildEffective/buildChangedOnly/saveIni estendidos; runDeliveryRoutingMenu implementada; main() tem "Roteamento de destino" no menu e chama runDeliveryRoutingMenu
- tests/run.lua: 4 testes de roteamento inseridos na tabela tests, todos passando
- Sem regressoes nos testes existentes
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/20-roteamento-multi-destino/20-02-SUMMARY.md` seguindo o template em @P:\Minecraft\ColonyFlow\.planning\phases\20-roteamento-multi-destino\.
</output>
