---
phase: 20-roteamento-multi-destino
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/config.lua
  - modules/engine.lua
autonomous: true
requirements:
  - phase-20
must_haves:
  truths:
    - "Itens com classe mapeada e periferico online sao roteados para o inventario configurado"
    - "Itens com classe mapeada mas periferico offline caem no default_target_container"
    - "Itens sem classe ou sem mapeamento caem no default_target_container"
    - "Se default_target_container offline, comportamento atual (waiting_retry) e preservado"
    - "Saude exibe 'Targets: X/Y online' somando default + rotas configuradas"
  artifacts:
    - path: "lib/config.lua"
      provides: "Secao [delivery_routing] com 11 chaves vazias nos defaults"
      contains: "[delivery_routing]"
    - path: "modules/engine.lua"
      provides: "resolveRoutedTarget() + logica per-request no tick"
      exports: ["resolveRoutedTarget"]
  key_links:
    - from: "modules/engine.lua:resolveRoutedTarget"
      to: "lib/config.lua:[delivery_routing]"
      via: "cfg:get(\"delivery_routing\", class, \"\")"
      pattern: "cfg:get.*delivery_routing"
    - from: "modules/engine.lua:tick loop"
      to: "resolveRoutedTarget"
      via: "called per-request inside _processRequest ctx override"
      pattern: "resolveRoutedTarget"
    - from: "modules/engine.lua:buildPeripheralHealth"
      to: "delivery_routing config keys"
      via: "ROUTING_CLASSES iteration + peripheral.isPresent"
      pattern: "Targets:.*online"
---

<objective>
Implementar o nucleo do roteamento multi-destino: defaults de config, funcao resolveRoutedTarget, insercao per-request no loop do tick e atualizacao do health display.

Purpose: Sem esses cambos o sistema continua usando um unico destino para todos os items. Esta plano entrega a logica de selecao de destino por classe de item com fallback correto (D-01, D-02, D-03, D-04, D-05, D-07).

Output: lib/config.lua com secao [delivery_routing]; modules/engine.lua com resolveRoutedTarget, roteamento per-request no tick e health display atualizado.
</objective>

<execution_context>
@P:\Minecraft\ColonyFlow\.planning\phases\20-roteamento-multi-destino\20-CONTEXT.md
@P:\Minecraft\ColonyFlow\.planning\phases\20-roteamento-multi-destino\20-PATTERNS.md
</execution_context>

<context>
@P:\Minecraft\ColonyFlow\.planning\ROADMAP.md

<interfaces>
<!-- Interfaces extraidas dos arquivos que serao modificados. -->

From modules/engine.lua (funcoes existentes — NAO modificar assinaturas):

```lua
-- linha 116 — guessClass: classificar item em uma das 11 classes ou nil
local function guessClass(name)
  -- retorna "armor_helmet"|"armor_chestplate"|"armor_leggings"|"armor_boots"|
  --         "tool_pickaxe"|"tool_shovel"|"tool_axe"|"tool_hoe"|"tool_sword"|"tool_bow"|"tool_shield"
  -- ou nil quando nao reconhece o nome
end

-- linha 167 — resolveTarget: fallback para default_target_container
local function resolveTarget(cfg)
  -- retorna (targetName, targetInv) ou (nil, nil)
end

-- linha 217 — getDestinationSnapshot: cache parametrizado por targetName
local function getDestinationSnapshot(state, targetName, targetInv, forceRefresh)
  -- retorna (snap, err); usa state.cache:get("dest", targetName) internamente
end

-- linha 1009 — _processRequest: recebe ctx com campos abaixo
function Engine:_processRequest(r, ctx)
  -- ctx.available   table[string]->number  — alocacao por item (shared por tick)
  -- ctx.buildings   table
  -- ctx.citizens    table
  -- ctx.snap        table|nil              — snapshot do inventario de destino
  -- ctx.snapErr     string|nil
  -- ctx.targetName  string                 — nome do periferico de destino
  -- ctx.targetInv   userdata               — periferico wrappado
  -- ctx.nowEpoch    number
end
```

From lib/config.lua (API de leitura de config — sem mudancas):

```lua
function Config:get(section, key, default)   -- retorna string ou default
function Config:getList(section, key, default, sep)  -- retorna table de strings
function Config:getNumber(section, key, default)     -- retorna number ou default
```

Tick loop (modules/engine.lua:1158) — contexto de onde a mudanca deve ocorrer:

```lua
-- Estado ANTES da mudanca (linha 1158):
local targetName, targetInv = resolveTarget(state.cfg)
if not targetInv then
  self:_markAllWaitingRetry(requests, "destino_indisponivel")
  ...
end
local snap, snapErr = getDestinationSnapshot(state, targetName, targetInv, false)
-- ... construcao do ctx unico para todos os requests
local ctx = {
  available  = available,
  buildings  = buildings,
  citizens   = citizens,
  snap       = snap,
  snapErr    = snapErr,
  targetName = targetName,
  targetInv  = targetInv,
  nowEpoch   = os.epoch("utc"),
}
-- _processRequest(r, ctx) chamado para cada request com o mesmo ctx
```

Health block (modules/engine.lua:637-663):

```lua
-- Estado ANTES da mudanca:
local targetValue, targetLevel = "NA", "unknown"
if cfg and type(cfg.getList) == "function" and ... then
  local _, targetInv = resolveTarget(cfg)
  if targetInv then targetValue, targetLevel = "Online", "ok"
  else          targetValue, targetLevel = "Offline", "bad"
  end
end
return {
  ...
  { label = "Target", value = targetValue, level = targetLevel },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar defaults [delivery_routing] em lib/config.lua</name>
  <files>lib/config.lua</files>
  <read_first>
    - lib/config.lua (ler todo o arquivo — e curto, 383 linhas — para ver o fim do bloco DEFAULT_INI antes de editar)
  </read_first>
  <behavior>
    - Apos inserir a secao, cfg:get("delivery_routing", "armor_helmet", "") deve retornar ""
    - Apos inserir a secao, cfg:get("delivery_routing", "tool_pickaxe", "") deve retornar ""
    - A secao deve aparecer no arquivo com parseIni sem erros
  </behavior>
  <action>
Localizar o fim do bloco DEFAULT_INI em lib/config.lua. O bloco termina com:

```
requests_refresh_interval_seconds=5
]]
```

Inserir imediatamente antes do `]]` (fechamento do heredoc) a secao abaixo. NAO adicionar comentarios Lua dentro do heredoc — o parser leria como valor:

```
[delivery_routing]
armor_helmet=
armor_chestplate=
armor_leggings=
armor_boots=
tool_pickaxe=
tool_shovel=
tool_axe=
tool_hoe=
tool_sword=
tool_bow=
tool_shield=
```

Cada chave tem valor vazio (sentinela para "usa default_target_container"). Isso garante que cfg:get("delivery_routing", cls, "") retorne "" para qualquer classe nao configurada, ativando o fallback (D-02).

Nao criar lib/config_schema.lua nem alterar Schema — o GOAL.md lista lib/config_schema.lua como "files likely touched" mas o CONTEXT.md nao tem decisao para validacao de perifericos de roteamento. Os campos sao strings livres (nome de periferico); a validacao existente em Schema.validateUpdates nao cobre delivery_routing e nao deve ser bloqueante aqui.
  </action>
  <verify>
    <automated>cd /d P:\Minecraft\ColonyFlow && lua -e "local f=io.open('lib/config.lua','r'); local s=f:read('*a'); f:close(); assert(s:find('%[delivery_routing%]'), 'secao ausente'); assert(s:find('armor_helmet='), 'armor_helmet ausente'); assert(s:find('tool_shield='), 'tool_shield ausente'); print('OK')"</automated>
  </verify>
  <done>lib/config.lua contem a secao [delivery_routing] com as 11 chaves (armor_helmet ate tool_shield) com valores vazios antes do fechamento ]] do heredoc DEFAULT_INI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar resolveRoutedTarget + roteamento per-request + health update em modules/engine.lua</name>
  <files>modules/engine.lua</files>
  <read_first>
    - modules/engine.lua linhas 167-230 (resolveTarget, resolveInvByName, getDestinationSnapshot — ponto de insercao da nova funcao)
    - modules/engine.lua linhas 625-665 (buildPeripheralHealth — ponto de mudanca do health display)
    - modules/engine.lua linhas 1140-1220 (tick loop — ponto de mudanca da resolucao de destino)
    - modules/engine.lua linhas 1009-1115 (_processRequest — para entender como ctx e consumido)
  </read_first>
  <behavior>
    - resolveRoutedTarget(cfg, "iron_helmet") com armor_helmet="rack_tools_0" e rack_tools_0 online deve retornar ("rack_tools_0", peripheral.wrap("rack_tools_0"))
    - resolveRoutedTarget(cfg, "iron_helmet") com armor_helmet="rack_tools_0" e rack_tools_0 OFFLINE deve chamar resolveTarget(cfg)
    - resolveRoutedTarget(cfg, "minecraft:bread") — guessClass retorna nil — deve chamar resolveTarget(cfg)
    - resolveRoutedTarget(cfg, "iron_pickaxe") com tool_pickaxe="" deve chamar resolveTarget(cfg)
    - Health: label "Target" substituido por "Targets" com valor "X/Y online"
  </behavior>
  <action>
**Passo A — Adicionar resolveRoutedTarget (imediatamente apos a definicao de resolveTarget, linha ~180):**

```lua
-- Resolve o inventario de destino roteado por classe de item (D-01, D-02, D-03).
-- Se a classe do item tiver periferico configurado e online, usa esse periferico.
-- Caso contrario, cai em resolveTarget(cfg) (default_target_container).
local function resolveRoutedTarget(cfg, itemName)
  local class = guessClass(itemName)
  if class then
    local routedName = cfg:get("delivery_routing", class, "")
    if routedName ~= "" and peripheral.isPresent(routedName) then
      return routedName, peripheral.wrap(routedName)
    end
  end
  return resolveTarget(cfg)   -- fallback: D-01 (offline), D-02 (nao mapeado), D-03 (default offline)
end
```

**Passo B — Modificar o tick loop (linhas ~1158-1201):**

O loop atual resolve UM destino compartilhado para todos os requests antes do loop de requests. Com roteamento por classe, cada request precisa do seu proprio destino. A mudanca e:

1. Manter a resolucao pre-loop de `resolveTarget(state.cfg)` como verificacao de saude geral (se nem o default esta disponivel, marcar todos em waiting_retry — comportamento atual preservado).
2. Dentro do loop de requests, antes de chamar `_processRequest`, resolver o destino especifico do request usando `resolveRoutedTarget` e montar um `ctx` per-request com os campos atualizados.

Substituir o bloco atual (linhas ~1158-1201) pelo seguinte:

```lua
-- Verificacao de saude: se nem o default_target_container esta online, aborta o tick
local defaultTargetName, defaultTargetInv = resolveTarget(state.cfg)
if not defaultTargetInv then
  self:_markAllWaitingRetry(requests, "destino_indisponivel")
  publishSnapshot(state)
  self:_persistWorkMaybe()
  return
end

-- Snapshot pre-calculado do destino default (reutilizado quando nao ha rota especifica)
local defaultSnap, defaultSnapErr = getDestinationSnapshot(state, defaultTargetName, defaultTargetInv, false)
if defaultSnap == nil and isBudgetExceeded(defaultSnapErr) then
  publishSnapshot(state)
  self:_persistWorkMaybe()
  return
end

local buildings = self:_fetchBuildings()
if buildings == nil then
  publishSnapshot(state)
  self:_persistWorkMaybe()
  return
end

local citizens = self:_fetchCitizens()
if citizens == nil then
  publishSnapshot(state)
  self:_persistWorkMaybe()
  return
end

-- available e compartilhado entre requests do mesmo tick para evitar over-allocation
local available = {}
if type(defaultSnap) == "table" then
  for k, v in pairs(defaultSnap) do available[k] = tonumber(v or 0) or 0 end
end

-- ctx base — sera sobrescrito por campos per-request antes de _processRequest
local baseCtx = {
  available  = available,
  buildings  = buildings,
  citizens   = citizens,
  nowEpoch   = os.epoch("utc"),
}
```

E dentro do loop de requests (onde `_processRequest(r, ctx)` e chamado), substituir a chamada por:

```lua
-- Resolve destino especifico para este request (D-01, D-02, D-05)
local reqItemName = (r.accepted and r.accepted[1] and r.accepted[1].name)
                 or (r.items and r.items[1] and r.items[1].name)
                 or ""
local routedName, routedInv = resolveRoutedTarget(state.cfg, reqItemName)
local routedSnap, routedSnapErr
if routedName == defaultTargetName then
  -- Mesmo destino que o default: reutiliza snapshot (D-04, D-05)
  routedSnap    = defaultSnap
  routedSnapErr = defaultSnapErr
else
  -- Destino roteado diferente: snapshot on-demand com cache por nome (D-04, D-05)
  routedSnap, routedSnapErr = getDestinationSnapshot(state, routedName or defaultTargetName,
                                                      routedInv or defaultTargetInv, false)
end

local ctx = {
  available  = baseCtx.available,
  buildings  = baseCtx.buildings,
  citizens   = baseCtx.citizens,
  snap       = routedSnap,
  snapErr    = routedSnapErr,
  targetName = routedName or defaultTargetName,
  targetInv  = routedInv or defaultTargetInv,
  nowEpoch   = baseCtx.nowEpoch,
}
local did, budgetErr = self:_processRequest(r, ctx)
```

ATENCAO: Remover a linha original `local did, budgetErr = self:_processRequest(r, ctx)` que existia antes e substituir pelo bloco acima. Manter todo o resto do loop (cursor, scanned/processed, break em budget) inalterado.

**Passo C — Atualizar buildPeripheralHealth (linhas ~637-663):**

Substituir o bloco do "Target" (linhas ~637-650) por:

```lua
local ROUTING_CLASSES = {
  "armor_helmet","armor_chestplate","armor_leggings","armor_boots",
  "tool_pickaxe","tool_shovel","tool_axe","tool_hoe","tool_sword","tool_bow","tool_shield"
}
local targetsTotal, targetsOnline = 0, 0
if cfg and type(cfg.get) == "function" and type(peripheral) == "table" and type(peripheral.isPresent) == "function" then
  -- contar default_target_container (D-07)
  local defaultName = trim(cfg:get("delivery", "default_target_container", ""))
  if defaultName ~= "" then
    targetsTotal = targetsTotal + 1
    if peripheral.isPresent(defaultName) then targetsOnline = targetsOnline + 1 end
  end
  -- contar destinos roteados nao-vazios (D-07)
  for _, cls in ipairs(ROUTING_CLASSES) do
    local rn = trim(cfg:get("delivery_routing", cls, ""))
    if rn ~= "" then
      targetsTotal = targetsTotal + 1
      if peripheral.isPresent(rn) then targetsOnline = targetsOnline + 1 end
    end
  end
end
local targetsValue = (targetsTotal > 0) and (targetsOnline .. "/" .. targetsTotal .. " online") or "NA"
local targetsLevel = (targetsTotal == 0 or targetsOnline == 0) and "bad" or
                     (targetsOnline < targetsTotal and "warn" or "ok")
```

E substituir a entrada `{ label = "Target", ... }` no return por:

```lua
{ label = "Targets", value = targetsValue, level = targetsLevel },
```

A variavel local `trim` ja existe em buildPeripheralHealth (ou na closure do modulo) — verificar e usar a mesma. Se nao existir localmente, usar `Util.trim(...)`.
  </action>
  <verify>
    <automated>cd /d P:\Minecraft\ColonyFlow && lua -e "local s=io.open('modules/engine.lua','r'):read('*a'); assert(s:find('resolveRoutedTarget'), 'funcao ausente'); assert(s:find('delivery_routing'), 'lookup de config ausente'); assert(s:find('Targets'), 'health label ausente'); assert(s:find('defaultTargetName'), 'verificacao default ausente'); print('OK')"</automated>
  </verify>
  <done>
    - resolveRoutedTarget esta definida logo apos resolveTarget em engine.lua
    - O tick loop usa resolveTarget como verificacao de saude e resolveRoutedTarget per-request
    - ctx e construido per-request com targetName/targetInv/snap do destino roteado
    - buildPeripheralHealth exibe "Targets: X/Y online" com contagem de default + roteados
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.ini -> engine | Nomes de perifericos lidos de arquivo local; um nome malicioso nao pode executar codigo (apenas string passada para peripheral.isPresent/wrap) |
| CC Lua sandbox | CC:Tweaked nao expoe sistema de arquivos do OS; peripheral.wrap com nome invalido retorna nil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Spoofing | resolveRoutedTarget — nome de periferico vindo de config | accept | CC:Tweaked sandbox: peripheral.wrap com nome invalido retorna nil; nenhum codigo e executado a partir do nome |
| T-20-02 | Denial of Service | Loop de requests com muitos destinos roteados offline | mitigate | resolveRoutedTarget faz peripheral.isPresent (rapido, nao bloqueia) antes de wrap; sem IO pesado no caminho feliz |
| T-20-03 | Information Disclosure | Logs de roteamento expondo nomes de perifericos | accept | Ambiente local single-player/LAN; logs sao para diagnostico operacional |
| T-20-04 | Elevation of Privilege | Escrita em config.ini via config_cli com nome de periferico arbitrario | accept | CC:Tweaked nao tem escalada de privilegio via nomes de periferico; peripheral.wrap com nome invalido retorna nil |
</threat_model>

<verification>
Verificacoes gerais da fase apos execucao de ambos os planos:

1. lua -e "..." em lib/config.lua confirma presenca das 11 chaves em [delivery_routing]
2. lua -e "..." em modules/engine.lua confirma resolveRoutedTarget, Targets e defaultTargetName
3. lua -e "..." em modules/config_cli.lua confirma runDeliveryRoutingMenu e "Roteamento de destino"
4. lua -e "..." em tests/run.lua confirma presenca dos 4 testes de roteamento
5. startup test no CC retorna todos os testes de roteamento como [OK]
</verification>

<success_criteria>
- lib/config.lua: secao [delivery_routing] com 11 chaves vazias presente no DEFAULT_INI
- modules/engine.lua: resolveRoutedTarget definida, chamada per-request no tick loop, health exibe "Targets: X/Y online"
- Fallback correto: classe offline -> default; classe nao mapeada -> default; default offline -> waiting_retry
- Cache de destino por nome reutilizado (sem IO redundante para mesmo destino no mesmo tick)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/20-roteamento-multi-destino/20-01-SUMMARY.md` seguindo o template em @P:\Minecraft\ColonyFlow\.planning\phases\20-roteamento-multi-destino\.
</output>
