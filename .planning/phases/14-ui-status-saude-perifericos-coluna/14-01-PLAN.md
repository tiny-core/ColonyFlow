---
phase: 14-ui-status-saude-perifericos-coluna
plan: 01
type: execute
wave: 1
depends_on: ["12-01"]
requirements_addressed: []
files_modified: [
  modules/peripherals.lua,
  modules/me.lua,
  modules/engine.lua,
  components/ui.lua,
  tests/run.lua,
  .planning/phases/14-ui-status-saude-perifericos-coluna/14-VERIFICATION.md
]
autonomous: true
must_haves:
  truths:
    - Monitor 2 (Status) exibe uma coluna alinhada com status dos perifericos essenciais, lado a lado com contadores.
    - ME Bridge mostra "Online/Offline" baseado em `ME:isOnline()` e usa cores (verde/vermelho/cinza).
    - Calculo de saude nao spamma perifericos no tick da UI (usa cache/snapshot).
    - ASCII-only nos textos exibidos.
  artifacts:
    - components/ui.lua
    - modules/engine.lua (ou outro ponto de snapshot)
    - .planning/phases/14-ui-status-saude-perifericos-coluna/14-VERIFICATION.md
---

# Phase 14 Plan 01: UI Status - Saude de perifericos (coluna alinhada)

## Objective

- **What:** Adicionar uma coluna/tabela no Monitor 2 (Status) exibindo perifericos essenciais com cor e alinhamento vertical.
- **Why:** Diagnostico rapido (ME/colony/modem/monitores) sem abrir logs/terminal.
- **Output:** Snapshot de saude + layout em colunas na UI + verificacao.

## Tasks

<task type="code">
  <name>Criar snapshot de saude de perifericos (cacheado)</name>
  <files>modules/engine.lua (ou novo modulo), modules/me.lua</files>
  <read_first>
    - modules/me.lua
    - modules/engine.lua
    - lib/cache.lua
  </read_first>
  <action>
    - Criar/atualizar `state.health` (ex.: `state.health.peripherals`) com status:
      - me: ok(bool), label("Online/Offline"), reason(opcional)
      - colonyIntegrator: presente/ausente
      - modem: presente/ausente
      - monitorRequests: presente/ausente
      - monitorStatus: presente/ausente
    - Atualizar esse snapshot em intervalos (ex.: TTL 2s) usando `state.cache` para reduzir chamadas.
    - Para ME, usar `ME:isOnline()` (grid), nao apenas presence.
  </action>
  <verify>
    - `startup test`
  </verify>
</task>

<task type="code">
  <name>UI: renderizar coluna de perifericos lado a lado com contadores</name>
  <files>components/ui.lua</files>
  <read_first>
    - components/ui.lua
  </read_first>
  <action>
    - Na secao OPERACAO do Monitor 2:
      - Transformar os contadores atuais em uma tabela de 2 colunas: "Contadores" | "Perifericos"
      - Cada linha deve ter `label: value` e o valor do periferico deve ser colorido:
        - verde para online/presente
        - vermelho para offline/ausente
        - cinza para desconhecido
      - Garantir alinhamento vertical e truncamento conforme width.
  </action>
  <verify>
    - Manual: ver 14-VERIFICATION.md
  </verify>
</task>

<task type="code">
  <name>Testes: validar funcao de layout/formatacao sem peripherals reais</name>
  <files>tests/run.lua</files>
  <read_first>
    - tests/run.lua
    - components/ui.lua
  </read_first>
  <action>
    - Extrair helper de layout (string formatting) para ser testavel, ou testar funcao pura nova.
    - Casos: largura pequena, coluna alinhada, labels truncados.
  </action>
  <verify>
    - `startup test`
  </verify>
</task>
